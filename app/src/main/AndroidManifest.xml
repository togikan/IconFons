<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kayat.fontello" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/tilki"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.BankingActivity"></activity>
        <activity android:name=".activities.PinActivity"></activity>
        <activity android:name=".activities.TransfersActivity"></activity>
        <activity android:name=".activities.AccountsActivity"></activity>
        <activity android:name=".activities.LoginActivity"></activity>
    </application>

</manifest>
