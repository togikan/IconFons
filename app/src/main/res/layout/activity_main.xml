<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/banking1">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <com.example.kayat.fontello.IconTextView
            android:id="@+id/main_mobile_banking"
            style="@style/MainMenuIcon"
            android:layout_weight="1"
            app:text="@string/mobile_banking"
            app:iconText="\ue010"
            app:iconSize="@integer/main_menu_icon_size"
            app:textSize="@integer/main_menu_text_size"
            app:alignment="vertical"/>

        <com.example.kayat.fontello.IconTextView
            android:id="@+id/main_mobile_pin"
            style="@style/MainMenuIcon"
            android:layout_weight="1"
            app:text="@string/mobile_pin"
            app:iconText="\ue007"
            app:iconSize="@integer/main_menu_icon_size"
            app:textSize="@integer/main_menu_text_size"
            app:alignment="vertical"/>

        </LinearLayout>

</LinearLayout>
